mod scanner;
mod common;
mod token;
mod keywords;
mod lexer;
mod parser;
mod symtab;
mod symbol;
mod plat;
mod gen_ir;
mod intercode;

use failure;

pub fn run() -> Result<(), failure::Error> {

    Ok(())
}